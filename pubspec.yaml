name: desafio_mobile
description: Desafio Mobile - Flutter
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  url_launcher: ^6.2.0
  http: ^1.1.0  
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true